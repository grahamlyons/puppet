# CentOS
#   su -c 'rpm -Uvh http://download.fedoraproject.org/pub/epel/6/i386/epel-release-6-7.noarch.rpm'
#   sudo -E yum install puppet
#
# Ubuntu
#   sudo -E aptitude install puppet
#
# sudo ssh-keygen -t rsa -f /home/git/.ssh/id_rsa-git.com
