[program:<%= @name %>]
directory=/var/apps/<%= @name %>/
environment=NEW_RELIC_CONFIG_FILE="newrelic.ini"
command=newrelic-admin run-program gunicorn server:app -b 127.0.0.1:<%= @port %> -k gevent --log-syslog
